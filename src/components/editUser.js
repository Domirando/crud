// export default function editUser(){
//     const editUser = (id) => {
//         users.filter(user => user.id === id ? console.log(user.name) : '')
//         setEdit(true);
//         let editingUser = users.find(user => {
//             return user.id === id;
//         })
//         setEditingUser({
//             name: editingUser.name,
//             email: editingUser.email,
//             title: editingUser.title
//         })
//     }
//     return (
//
//     )
// }
//
